using GreenChat.Client_WEB.Controllers;
using GreenChat.Client_WEB.Models.AccountViewModels;

namespace GreenChat.Client_WEB.Models
{
    /*
    public class AutoMapperProfile : Profile
    {
        public AutoMapperProfile()
        {
            CreateMap<LoginViewModel, UserDto>();
            CreateMap<UserDto, LoginViewModel>();
            
            CreateMap<RegisterViewModel, UserDto>().ForMember(obj => obj.UserName, m=>m.MapFrom(s=>s.Email));
            CreateMap<UserDto, RegisterViewModel>();
        }
    }
    */
}